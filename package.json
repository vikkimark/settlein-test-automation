{
  "name": "settlein-test-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end test automation for the SettleIn app (test suite only, no app source)",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
